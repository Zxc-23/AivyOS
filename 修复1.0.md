# AivyOS 修复说明 v1.0

> 修复日期：2026-08-25
> 分支：`feature/comprehensive_test_v1.0`
> 依据：全面 bug 检查（代码审查 + 静态扫描 + 全量测试）
> 全量测试：**607 个测试全部通过，跳过 9 个**

---

## 一、已完成修复（4 项）

### 1. config.py：重复 `emotion` 键导致默认配置丢失 🔴 高

- **文件**：`aivyos_core/config.py`
- **问题**：`DEFAULT_CONFIG` 中 `emotion` 键定义了两次（原 133-138 行与 203-205 行）。Python 字典后定义覆盖先定义，导致第一处的 `enabled`、`auto_detect`、`default_emotion` 配置完全丢失，情感控制功能实际运行时仅剩 `tags_enabled=True`。
- **修复**：合并两个配置块为单一 `emotion` 字典，保留全部字段：

```python
"emotion": {
    "enabled": True,
    "auto_detect": True,          # 自动从文本关键词推断情感
    "default_emotion": "neutral", # 无关键词时的默认情感
    "tags_enabled": True,         # §6.1 14 种细粒度情感标签
},
```

---

### 2. workbench 分发器：命令注入风险 🔴 高

- **文件**：`aivyos_core/workbench/dispatchers/base.py`、`claude_code.py`、`codex.py`
- **问题**：`run_cli` 使用 `asyncio.create_subprocess_shell` 执行拼接的命令字符串；`claude_code.py` / `codex.py` 用 `" ".join(cmd_parts)` 拼接命令。若 `cli_path` 或 `task.extra_args` 含空格/引号/特殊字符，可执行任意命令。
- **修复**：
  - `base.py` 的 `run_cli` 支持 `str` 与 `List[str]` 两种命令类型；传列表时改用 `asyncio.create_subprocess_exec`（无 shell，杜绝注入），字符串分支仅保留向后兼容。
  - `claude_code.py` 改为直接传 `cmd_parts` 列表。
  - `codex.py` 改为直接传 `cmd` 列表。
  - 修正 `cmd_display` 在 `FileNotFoundError` 分支的 `UnboundLocalError`（提前计算显示字符串）。
- **测试适配**：`tests/test_workbench_claude_dispatcher.py` 新增 `_normalize_cmd` 与 `_patch_subprocess`（同时 mock shell 与 exec），`tests/test_workbench_codex_dispatcher.py` 同步更新。

---

### 3. api_key_store.py：不安全的 XOR 回退加密 🟡 中

- **文件**：`aivyos_core/api_key_store.py`
- **问题**：`cryptography` 不可用时退化为纯 XOR+base64，极易破解；且无完整性校验，密文可被篡改而不自知。
- **修复**（仅使用 stdlib，不新增依赖）：
  - **PBKDF2-HMAC-SHA256**（10 万次迭代）从机器指纹 + 每条目随机 16 字节 salt 派生 64 字节密钥材料（前 32 字节加密、后 32 字节 HMAC）。
  - 新格式：`base64(salt[16] || ciphertext || hmac[32])`，每个条目独立 salt，杜绝跨条目关联。
  - **HMAC-SHA256** 完整性校验，防篡改；验证失败返回空串并告警。
  - **向后兼容**：新增 `_legacy_decrypt` 解密旧版纯 XOR 格式，已有存储文件可正常读取；下次写入自动升级为新格式。

---

### 4. voice/session.py：使用已弃用的 `asyncio.get_event_loop()` 🟡 中

- **文件**：`aivyos_core/voice/session.py`
- **问题**：`start_ptt` 中 `asyncio.get_event_loop().create_task(...)`，Python 3.10+ 已弃用，3.12+ 在运行中的协程内调用可能抛 `RuntimeError`。
- **修复**：改用 `asyncio.create_task(...)`（内部使用当前运行循环）。

---

## 二、未处理/遗留事项

| 项 | 说明 | 建议 |
|---|---|---|
| `except Exception: pass` 静默吞异常 | 全库约 47 处，多为优雅降级设计，但可能掩盖真实错误 | 至少加 `log.debug` 记录；需逐处评估是否属于容错预期 |
| `run_cli` 字符串分支仍走 shell | 仅向后兼容旧调用保留；现有分发器均已改为列表传参 | 后续彻底移除字符串分支或强制 `shlex.quote` |
| XOR 回退方案强度有限 | 相比 Fernet（AES-CTR）仍较弱，PBKDF2 仅提升破解成本 | 建议将 `cryptography` 纳入正式依赖，启用 Fernet 完整路径 |
| `_simple_decrypt` HMAC 失败静默返回空串 | 防篡改已生效但用户无感知 | 可考虑在 UI/日志层提示密钥文件被篡改 |

---

## 三、验证结果

- **针对性测试**（config / persona / voice / workbench 分发器 / provider_store）：50 个测试全部通过
- **全量测试**：`python -m unittest discover -s tests -v` → **607 passed, 9 skipped**，耗时约 120s
- **静态检查**：全部 `.py` 文件 `py_compile` 通过，无语法错误

---

## 四、改动文件清单

| 文件 | 改动类型 |
|---|---|
| `aivyos_core/config.py` | 修复重复键 |
| `aivyos_core/workbench/dispatchers/base.py` | 命令注入修复 + UnboundLocalError 修复 |
| `aivyos_core/workbench/dispatchers/claude_code.py` | 列表传参 |
| `aivyos_core/workbench/dispatchers/codex.py` | 列表传参 |
| `aivyos_core/api_key_store.py` | 加密强化（PBKDF2 + HMAC + salt）|
| `aivyos_core/voice/session.py` | 事件循环调用修复 |
| `tests/test_workbench_claude_dispatcher.py` | 测试适配 |
| `tests/test_workbench_codex_dispatcher.py` | 测试适配 |
